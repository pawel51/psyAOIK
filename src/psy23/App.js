import React from 'react';
import ProductList from "./ProductList";

const App = props => {
    return (
        <div>
            <ProductList/>
        </div>
    );
};

App.propTypes = {
    
};

export default App;